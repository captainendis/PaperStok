<!--
  Copyright (c) 2026 PaperAxis. All rights reserved.
  This file is part of PaperStok.
-->
<Window x:Class="PaperStok.App.Views.TextInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PaperStok"
        Width="380" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <StackPanel Margin="24">
        <TextBlock x:Name="PromptText" TextWrapping="Wrap" Margin="0,0,0,8" />
        <TextBox x:Name="ValueBox" Margin="0,0,0,4" />
        <TextBlock x:Name="ErrorText" Foreground="{StaticResource Pa.Danger}" FontSize="11"
                   TextWrapping="Wrap" Visibility="Collapsed" Margin="0,0,0,12" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="İptal" Style="{StaticResource Pa.SecondaryButton}" Click="Cancel_Click" Margin="0,0,8,0" />
            <Button Content="Tamam" Style="{StaticResource Pa.PrimaryButton}" Click="Ok_Click" />
        </StackPanel>
    </StackPanel>
</Window>
